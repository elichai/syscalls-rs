language: rust
cache: false
matrix:
  include:
  - os: linux
    arch: amd64
  - os: linux
    arch: arm64
  - os: linux-ppc64le

rust:
  - nightly

script:
  - cargo build --verbose
  - cargo test --verbose

notifications:
  email:
    on_success: never
